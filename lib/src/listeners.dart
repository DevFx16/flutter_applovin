enum AppLovinAdLoadListener { adReceived, failedToReceiveAd }
enum AdDisplayListener { adDisplayed, adHidden }
enum AdClickListener { adClicked }
enum AdViewEventListener {
  adOpenedFullscreen,
  adClosedFullscreen,
  adLeftApplication,
  adFailedToDisplay
}

class Listeners {}
